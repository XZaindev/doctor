{
  "version": 2,
  "buildCommand": "npm --prefix web-app install && npm --prefix web-app run build",
  "outputDirectory": "web-app/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
